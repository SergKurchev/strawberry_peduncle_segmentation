# Synthetic Dataset Generation для Instance Segmentation и Object Relationships

Система генерации синтетического датасета в Unity для обучения нейросетей определению связей между объектами.

## 📊 Структура annotations.json

### Общая структура COCO-формата

```json
{
  "images": [...],
  "categories": [...],
  "annotations": [...]
}
```

### 1. Images (Информация об изображениях)

```json
{
  "id": 0,           // Уникальный ID изображения
  "file_name": "00000.png",
  "width": 1024,     // Ширина изображения (может меняться при кропе)
  "height": 1024     // Высота изображения
}
```

### 2. Categories (Категории объектов)

```json
{
  "id": 1,                      // ID категории
  "name": "red_cube"           // Красный куб (3×3×3 см)
},
{
  "id": 2,
  "name": "green_parallelepiped"  // Зелёный параллелепипед (0.3×0.3×2 см)
}
```

### 3. Annotations (Аннотации объектов)

**Ключевые поля:**

```json
{
  "id": 1,                    // Уникальный ID аннотации
  "image_id": 0,              // ID изображения из раздела "images"
  "category_id": 1,           // ID категории (1=куб, 2=параллелепипед)
  
  // === ПОЛЯ ДЛЯ СВЯЗЕЙ ===
  "instance_id": 1,           // Уникальный ID экземпляра объекта в сцене
  "parent_id": 0,             // ID родительского объекта (0 для кубов, instance_id куба для параллелепипедов)
  
  // === ГЕОМЕТРИЯ ===
  "bbox": [x, y, width, height],  // Bounding box в формате COCO
  "area": 530.34,              // Площадь видимой части объекта (в пикселях)
  
  // === СЕГМЕНТАЦИЯ ===
  "segmentation_color": [R, G, B], // Цвет объекта на маске:
                                    // R = instance_id (1-255)
                                    // G = category_id (1-255)
                                    // B = 0
  
  // === ОККЛЮЗИЯ ===
  "visibility_ratio": 0.95     // Процент видимости объекта (0.0-1.0)
                              // Учитывает перекрытие другими объектами
}
```

### Пример связи объектов

**Куб:**
```json
{
  "id": 1,
  "category_id": 1,      // red_cube
  "instance_id": 5,
  "parent_id": 0,        // У куба нет родителя
  ...
}
```

**Параллелепипед на этом кубе:**
```json
{
  "id": 2,
  "category_id": 2,          // green_parallelepiped
  "instance_id": 5,          // Тот же instance_id!
  "parent_id": 5,            // Ссылка на instance_id родительского куба
  ...
}
```

**Важно:** `parent_id` параллелепипеда = `instance_id` родительского куба. Это позволяет однозначно определить связь.

---

## 🧠 Логика Train_segmentation.ipynb

### 1. **Загрузка и анализ датасета**

```python
# Анализ структуры
- Количество изображений, аннотаций
- Распределение по категориям
- Проверка связей parent_id
- Анализ visibility_ratio
```

**Что проверяется:**
- Корректность связей: для каждого `parent_id` должен существовать куб с таким `instance_id`
- Валидность данных: размеры изображений, количество объектов
- Распределение видимости: `visibility_ratio` показывает насколько хорошо объект виден

### 2. **Dataset класс**

```python
class CubeParallelepipedDataset:
    def __getitem__(self, idx):
        # Загрузка RGB изображения
        # Загрузка маски сегментации
        # Извлечение бинарных масок для каждого объекта
        # Возврат тензоров для PyTorch
```

**Как извлекаются маски:**
```python
seg_color = ann['segmentation_color']  # [R, G, B]
obj_mask = np.all(mask_image == seg_color, axis=2).astype(np.uint8)
```

Pixel-perfect маски извлекаются по цвету из цветового изображения маски.

### 3. **Модель: Mask R-CNN**

```python
# Предобученная ResNet50 + FPN
model = maskrcnn_resnet50_fpn(weights=MaskRCNN_ResNet50_FPN_Weights.DEFAULT)

# Заменяем головы под наши классы
- box_predictor → 3 класса (bg + 2 объекта)
- mask_predictor → 3 класса
```

**Почему Mask R-CNN:**
- Instance segmentation "из коробки"
- Предсказывает bbox, класс, маску для каждого объекта
- Уже обучена на COCO, transfer learning

### 4. **Предсказание связей**

```python
def predict_associations(boxes, labels, scores):
    # Для каждого параллелепипеда:
    #   1. Находим все кубы
    #   2. Вычисляем расстояние (вертикальное + горизонтальное)
    #   3. Выбираем ближайший куб НАД которым параллелепипед
    #   4. Бонус если параллелепипед по центру куба
```

**Логика эвристики:**
- Параллелепипед всегда НАД кубом (физика)
- Вычисляем `vertical_dist = |para_bottom - cube_top|`
- Вычисляем `horizontal_dist = |para_center_x - cube_center_x|`
- Общее расстояние: `sqrt(vertical^2 + horizontal^2)`
- Если параллелепипед горизонтально по центру куба → расстояние * 0.5 (приоритет)

### 5. **Метрики**

```python
def compute_association_accuracy(predictions, targets):
    # Для каждого корректно обнаруженного параллелепипеда (IoU > 0.5):
    #   1. Находим GT parent_id
    #   2. Предсказываем связь через predict_associations()
    #   3. Проверяем совпадение предсказанного куба с GT кубом (IoU > 0.5)
    #   4. Подсчитываем accuracy
```

**Почему двойная проверка IoU:**
- Первый IoU: параллелепипед правильно обнаружен
- Второй IoU: предсказанный родительский куб совпадает с GT

### 6. **Обучение**

```python
for epoch in range(NUM_EPOCHS):
    # Стандартный цикл Mask R-CNN
    loss = model(images, targets)  # Автоматически вычисляет все лоссы
    
    # Периодическая оценка связей
    if epoch % 5 == 0:
        association_accuracy = ...
```

**Важно:** Mask R-CNN обучается на segmentation/detection, а связи предсказываются **пост-обработкой** через эвристику на основе геометрии.

### 7. **Inference**

```python
# 1. Получаем детекции от Mask R-CNN
predictions = model(image)

# 2. Фильтруем по confidence
keep = scores >= threshold

# 3. Предсказываем связи
associations = predict_associations(boxes, labels, scores)

# 4. Визуализация с линиями связей
```

---

## 🚀 Как использовать

### 1️⃣ Генерация датасета (Unity)

```
1. Открыть Unity проект
2. Tools → Dataset Capture
3. Create BatchDatasetCapture on Main Camera
4. Play Mode
5. ▶ START BATCH CAPTURE
```

**Результат:**
- `strawberry_peduncle_segmentation/dataset/images/` (RGB)
- `strawberry_peduncle_segmentation/dataset/masks/` (маски)
- `strawberry_peduncle_segmentation/dataset/visualizations/` (визуализации)
- `strawberry_peduncle_segmentation/dataset/annotations.json`

### 2️⃣ Анализ датасета (Python)

```bash
cd strawberry_peduncle_segmentation
python dataset_stats.py
```

### 3️⃣ Визуализация масок

```bash
python visualize_masks.py
```

### 4️⃣ Обучение модели (Kaggle/Colab)

```bash
# Загрузить датасет на Kaggle
# Запустить train_segmentation.ipynb
```

---

## 📁 Структура проекта

```
My project/
├── Assets/
│   ├── Scripts/
│   │   ├── DatasetGenerator.cs      # Генерация сцен с объектами
│   │   ├── DatasetCapture.cs        # Quick capture (32 images)
│   │   ├── BatchDatasetCapture.cs   # Batch capture (1000 images)
│   │   └── Editor/
│   │       └── DatasetCaptureEditor.cs
│   └── Shaders/
│       └── SegmentationShader.shader
├── strawberry_peduncle_segmentation/
│   ├── dataset/
│   │   ├── images/          # RGB изображения
│   │   ├── masks/           # Цветовые маски сегментации
│   │   ├── visualizations/  # Усиленные маски для визуализации
│   │   └── annotations.json # COCO аннотации + связи
│   ├── dataset_stats.py     # Анализ датасета
│   └── visualize_masks.py   # Визуализация масок
└── train_segmentation.ipynb # Обучение Mask R-CNN

```

---

## ✨ Ключевые особенности

### Рандомизация камеры
- **Дистанция:** 0.6м - 1.2м (адаптивно под размер сцены)
- **Углы:** Азимут 0-360°, элевация 10-80°
- **Наклон:** Roll ±15°
- **Кропы:** 30% вероятность с минимальным размером 50%
- **Антиколлизия:** Проверка расстояния до объектов, увеличение радиуса при необходимости

### Фильтрация окклюзий
- Подсчёт реальных видимых пикселей на маске
- Объект включается в аннотации только если `visibility_ratio >= 0.3`
- Площадь (`area`) = количество видимых пикселей

### Связи объектов
- Каждый параллелепипед имеет `parent_id` → `instance_id` родительского куба
- Связь устанавливается автоматически при генерации
- Модель предсказывает связи через геометрическую эвристику

---

## 🔧 Настройки

### BatchDatasetCapture

```csharp
totalImages = 1000          // Количество изображений
imagesPerScene = 10         // Снимков с одной сцены
minOrbitRadius = 0.6f       // Минимальный радиус камеры
maxOrbitRadius = 1.2f       // Максимальный радиус
cropProbability = 0.3f      // Вероятность кропа
minVisibilityRatio = 0.3f   // Минимальная видимость для включения
```

### train_segmentation.ipynb

```python
NUM_EPOCHS = 30
BATCH_SIZE = 4
LEARNING_RATE = 0.005
TRAIN_RATIO = 0.8
```

---

## 📝 Примечания

1. **Размер параллелепипеда:** Изменён с 0.1×0.1×2 см до 0.3×0.3×2 см для лучшей видимости
2. **Маски тёмные:** Значения instance_id от 1-14 выглядят чёрными, используй визуализации
3. **parent_id совпадает с instance_id:** Оба объекта (куб и параллелепипед) имеют одинаковый instance_id
4. **Окклюзии учтены:** Если объ ект перекрыт более чем на 70%, он не попадает в аннотации

---

## 🎯 Цель проекта

Обучить нейросеть:
1. **Обнаруживать** красные кубы и зелёные параллелепипеды
2. **Сегментировать** каждый объект отдельно (instance segmentation)
3. **Определять связь** какой параллелепипед принадлежит какому кубу
